# Real Prelisten v1 Capsule

- Updated: 2026-08-04
- Owner: `apps/companion-app/src/prelisten/`; presentation/proof 继续由 Confirmation Trust v1 持有。
- Stable reuse: companion 只从公开 `@yimi-pen/audio` 复用 `AudioPlayer/AudioBackend`；`packages/audio/src`、Console backend、AudioQueue、provider 和 fixture transcript 保持。
- Flow: canonical WAV import → published bytes/hash/profile recheck → preview-time path/bytes/hash verification → pinned ffprobe → complete ffmpeg decode → app-local ffplay natural exit → explicit action → existing proof/provider/BuildAuthorization.
- Deterministic evidence: `build/companion-prelisten-validation/report.json`, 19/19, SHA-256 `0388020812fdbc45b79a13bbcb365d439056601faf437ffdfdde211a23819216`; early confirm, error, timeout, abort, cancel, missing receipt, duplicate/stale callback, tamper, path escape, cleanup barrier, resolve cancellation and concurrency included.
- Host process evidence: `build/companion-host-audio-validation/report.json`, 16/16, SHA-256 `46a2d097ff1c81c453156f84b7534acc1f5d32bdb3e61e4e0c2b2d44b2475a5c`; process settlement waits for true `close`, runtime errors retain ownership through cleanup, retry cannot overlap, and microphone staging/publish cleanup states are explicit.
- Host evidence: `build/companion-real-prelisten/report.json`; pinned suite hashes verified, 10 imports, 10 full decodes, 10/10 ffplay callbacks, proof verification and authorization passed after App-local orchestration extraction. Latest runner-action transcript semantic SHA-256 `c67fc7950c447d81a7f75ee6e7615a9cf92494f8870b220161c9b072ac46f3b2`; report file SHA-256 `5fac46f959a8d4ab0593ff8768a149563dcbe1b0fd809fe9eae4d3b7cb676c87`.
- Evidence boundary: runner action is not operator audibility; ffplay exit is not acoustic capture or target audio. The low-level prelisten recorder probe remains unbound; the authoring route now owns injected DirectShow capture→canonical import→FamilyRevision, with actual device/permission/run still a separate host gate.
- Failed paths preserved: Windows `fsync` on a read-only handle returned `EPERM`, fixed by flushing the owned staging file through `r+`; ffplay rejected `-nostdin`, so stdin isolation is provided by child stdio; rejecting only after awaited stop caused a watchdog hang; clearing the player during asynchronous cleanup let a stale stop affect retry; cancel during asset resolution could revive a session; a `.wav.partial-*` suffix did not reliably select the WAV muxer; `exitCode` was initially mistaken for the true `close` boundary.
- Routing boundary: this capsule belongs to the active software owner `tasks/system-product-rd/active-task.md`; hardware/EDA/采购/结构/供电/声学 remain external input evidence.
