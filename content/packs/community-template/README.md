# 社区主题包模板

1. 复制本目录，改名为你的 `pack id`
2. 编辑 `pack.meta.json`（许可证、作者、年龄）
3. 改 `books/*/book.json` 与 `audio/`
4. `npm run validate:books`（若合并进主 content 树）
5. 发 PR 或在自有仓库维护，并在 `registry.json` 登记

详见仓库根目录 [docs/pack-spec.md](../../../docs/pack-spec.md)。
